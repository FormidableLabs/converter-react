/**
 * Actions: Convert
 */
import alt from "../alt";

class ConvertActions {
  constructor() {
    this.generateActions(
      "fetchConversions"
    );
  }
}

export default alt.createActions(ConvertActions);
